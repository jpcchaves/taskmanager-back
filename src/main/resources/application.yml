spring:
  datasource:
    url: jdbc:mysql://localhost:3306/taskmanager?useTimezone=true&serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: admin123
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: MYSQL
    database-platform: org.hibernate.dialect.MySQL8Dialect
spring-doc:
  swagger-ui:
    operations-sorter: alpha
    tags-sorter: alpha

